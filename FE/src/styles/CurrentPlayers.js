import styled from "styled-components";

const Container = styled.div`
	padding: 20px 15px;
`;

const PlayerType = styled.div`
	font-size: 22px;
	margin-bottom: 7px;
`;

const PlayerInfo = styled.div`
	font-size: 17px;
	margin-bottom: 20px;
`;

export { Container, PlayerType, PlayerInfo };
